<!--<?php var_dump($data) ?>-->
<main>
    <h2 class="text-center m-5">Les Catégories</h2>
    <section class="d-flex flex-column align-items-center">
        <table class="table col-10 table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nom</th>
                    <th>Fonctions</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach($data as $category){ ?>
                <tr>
                    <td><?= $category->getId() ?></td>
                    <td><?= $category->getName() ?></td>
                    <td>
                        <a href="categorie/<?= $category->getId() ?>"><span class="fa-solid fa-eye"></span></a>
                        <a href="categorie/<?= $category->getId() ?>"><span class="fa-solid fa-pen"></span></a>
                        <a href="categorie/<?= $category->getId() ?>/supprimer"><span class="fa-solid fa-trash"></span></a>
                    </td>
                </tr>
                <?php } ?>
            </tbody>
        </table>
    </section>
</main>